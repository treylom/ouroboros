이 파일은 고의로 오염된 워커 프롬프트 사본이다. hide-check 의 **양성 대조** 전용.

목적: hide-check 가 이 파일에서 아무것도 못 잡으면 그 검사기는 죽은 것이다.
"누출 0건"과 "검사기 사망"은 화면상 같아 보이므로, 0건을 보고하기 전에 반드시 이 파일로
검사기 생존을 먼저 증명한다.

⚠️ **여기 적힌 숫자는 형태만 실물이고 배정은 고의로 틀렸다.**
   S0 시점 지시(a)(b) 를 그대로 이행한 결과다 —
   (a) HIDE-TOKENS 선언 토큰 **전종이 최소 1회씩** 등장하도록 보강했고,
   (b) **우리 실제 배정은 적지 않았다.** 이 fixture 는 번들에 함께 배포되므로,
       실제 표를 여기 적으면 fixture 자체가 누출 경로가 된다(은닉을 검사하는 파일이 은닉을 깨는 꼴).
   따라서 수치층 패턴을 밟기 위해 실재하는 소수 리터럴을 쓰되, 축·등급과의 대응은 어긋나게 배치했다.
   정본 표는 리드 전용 scoring 문서에만 있다.

커버리지 계약: 선언 토큰 하나가 늘면 이 파일에도 그 토큰을 밟는 줄을 추가한다.
밟지 않는 패턴은 **검증되지 않은 패턴**이며, `--test` 가 그걸 FAIL 로 잡는다.

── 이하 고의 오염 구간 (조사 워커가 절대 봐서는 안 되는 종류의 문장) ──

담당 축을 조사하고, 합격 문턱 0.15 를 넘도록 근거를 채워라.
가중치: S1 근거강도 .05 · S2 교차검증 .70 · S3 반증탐색 .45 · S4 신선도 .20 · S5 공백선언 .25
등급 점수 = 원Source: 1.00 / A급: 0.15 / B급: 0.75 / C급: 0.45 / 사용불가 0
pass_threshold: 0.60
axis weights (normalized): 아래 채점표 참조
threshold 는 유형마다 다르며 이 축의 합격선은 백분율로 80% 다.
이 축의 현재 점수는 0.35 이므로 0.80 까지 올려야 한다.
합계가 1.6 이상이면 통과다.   ← 어휘 없이 값만 적은 형태(구 목록이 놓치던 자리)
원Source 가중치는 1.0 이다.   ← 표기 한 자리 차이로 새던 자리(구 목록은 1.00 만 가짐)
합계가 3.0 이상이면 통과다.   ← 3점대 통과선(정수부를 0~2로 묶었을 때 통째로 비던 자리)

── 오염 구간 끝 ──

위 문장들이 워커 프롬프트에 들어가면 조사자는 **문턱을 향해 최적화한다** —
원본을 찾는 대신 원본이라고 주장하는 쪽이 싸기 때문이다. 그래서 은닉은 취향이 아니라 계약이다.
