질의: RFC 9110(HTTP Semantics)은 HTTP 상태코드 418("I'm a teapot")을 정식 상태코드로 등재하고 있는가?
담당 축: f3 — 반증: 주장에 반대되는 증거·반례를 적극적으로 찾는 축. 지지 축과 별개 조사 단위로 유지한다.
당신은 이 축만 조사한다. 다른 축은 다른 조사자가 맡고 있고, 그쪽 산출은 보지 않는다.

취득: references/acquisition.md 의 사다리를 위에서부터 시도한다.
  robots.txt·ToS·페이월·CAPTCHA 는 우회하지 않는다 — 미도달로 기록한다.
  2차 자료를 근거로 쓸 때는 원본 링크 추적을 1회 시도하고, 실패하면 "원본 미도달"이라 적는다.

반환(JSON):
{
  "frame_id": "f3",
  "findings": [ { "url": "", "claim": "", "verbatim": "", "accessed_at": "" } ],
  "self_report": {
    "S1": {"y": true, "why": "근거 1줄"},
    "S2": {"y": true, "why": "근거 1줄"},
    "S3": {"y": true, "why": "근거 1줄"},
    "S4": {"y": true, "why": "근거 1줄"},
    "S5": {"y": true, "why": "근거 1줄"}
  }
}

S1 근거강도 / S2 교차검증 / S3 반증탐색 / S4 신선도 / S5 공백선언 — 각 Y/N + 근거 1줄.
findings 에 등급 칸은 없다. 등급은 당신이 매기지 않는다.
원장 파일에 직접 쓰지 않는다 — 산출을 반환하면 병합은 리드가 한다.

마감: 2026-07-30 13:20 KST. 도달하면 그 시점 산출을 그대로 반환한다(미완도 반환).

재조사: 반증 축이 약하다. 반대 증거를 찾지 못했다.
반대 증거를 찾아라 — RFC 2324 계열 문서와 이후 개정 이력을 확인한다.
